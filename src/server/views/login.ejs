<!Doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex items-center justify-center">
    <div class="flex justify-center items-center min-h-screen bg-gray-900 text-gray-100">
        <div class="w-full max-w-md bg-gray-800 rounded-lg p-6">
            <h1 class="text-3xl font-bold text-center text-blue-400 mb-6">Login</h1>

            <!-- Display error message if login fails -->
            <% if (error) { %>
                <div class="bg-red-500 text-white p-4 rounded mb-4">
                    <%= error %>
                </div>
            <% } %>

            <!-- Login Form -->
            <form action="/auth/login" method="post">
                <div class="mb-4">
                    <label for="email" class="block text-sm font-bold mb-2">Email:</label>
                    <input id="email" name="email" type="email" required
                        class="w-full p-3 rounded bg-gray-700 border-none focus:outline-none text-white">
                </div>
                <div class="mb-6">
                    <label for="password" class="block text-sm font-bold mb-2">Password:</label>
                    <input id="password" name="password" type="password" required
                        class="w-full p-3 rounded bg-gray-700 border-none focus:outline-none text-white">
                </div>
                <button type="submit" class="w-full bg-blue-500 text-white p-3 rounded font-bold">
                    Login
                </button>
            </form>

            <!-- Sign-up Link -->
            <div class="mt-4 text-center">
                <p class="text-sm text-gray-300">
                    Don't have an account? 
                    <a href="/auth/register" class="text-green-400 hover:underline">Sign up here</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>
