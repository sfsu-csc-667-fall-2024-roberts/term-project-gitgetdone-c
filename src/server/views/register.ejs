<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Register to Play UNO</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen">
    <div class="flex justify-center items-center min-h-screen bg-gray-900 text-gray-100">
        <div class="w-full max-w-md bg-gray-800 rounded-lg p-6">
            <h1 class="text-3xl font-bold text-center text-green-400 mb-6">Register</h1>

            <!-- Display error message if registration fails -->
            <% if (error) { %>
                <div class="bg-red-500 text-white p-4 rounded mb-4">
                    <%= error %>
                </div>
            <% } %>

            <!-- Registration Form -->
            <form action="/auth/register" method="post">
                <div class="mb-4">
                    <label for="username" class="block text-sm font-bold mb-2">Username:</label>
                    <input id="username" name="username" type="text" required
                        class="w-full p-3 rounded bg-gray-700 border-none focus:outline-none text-white">
                </div>
                <div class="mb-4">
                    <label for="email" class="block text-sm font-bold mb-2">Email:</label>
                    <input id="email" name="email" type="email" required
                        class="w-full p-3 rounded bg-gray-700 border-none focus:outline-none text-white">
                </div>
                <div class="mb-6">
                    <label for="password" class="block text-sm font-bold mb-2">Password:</label>
                    <input id="password" name="password" type="password" required
                        class="w-full p-3 rounded bg-gray-700 border-none focus:outline-none text-white">
                </div>
                <button type="submit" class="w-full bg-green-500 text-white p-3 rounded font-bold">
                    Register
                </button>
            </form>

            <!-- Login Link -->
            <div class="mt-4 text-center">
                <p class="text-sm text-gray-300">
                    Already have an account? 
                    <a href="/auth/login" class="text-blue-400 hover:underline">Log in here</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>
